import React from "react";

const Home = () => {
  return (
    <div className="home-container">
      <h1>
        When you run this on your local computer, React scripts will handle the
        value of the PUBLIC_URL value. When you run it locally, it will look
        like a relative URL instead of absolute URL: When you run this on your
        local computer, React scripts will handle the value of the PUBLIC_URL
        value. When you run it locally, it will look like a relative URL instead
        of absolute URL: When you run this on your local computer, React scripts
        will handle the value of the PUBLIC_URL value. When you run it locally,
        it will look like a relative URL instead of absolute URL:
      </h1>
    </div>
  );
};

export default Home;
